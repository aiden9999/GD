<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="worry">
	<select id="worry" resultType="java.util.HashMap">
		select * from worry order by day desc limit 10 offset #{page}
	</select>
	<insert id="write" parameterType="java.util.HashMap">
		insert into worry(content, writer, name, day) values(#{worry}, #{id}, #{name}, now())
	</insert>
	<insert id="reply" parameterType="java.util.HashMap">
		insert into worryreply values(#{id}, #{name}, #{reply}, now(), #{num})
	</insert>
	<select id="replyList" resultType="java.util.HashMap">
		select * from worryreply order by day desc
	</select>
	<select id="worryList" resultType="java.util.HashMap">
		select * from worry order by day desc
	</select>
	<select id="worryCount" resultType="java.lang.Integer">
		select count(*) from worry
	</select>
</mapper>