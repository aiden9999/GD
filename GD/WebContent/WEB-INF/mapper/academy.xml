<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="academy">
	<insert id="newAcademy" parameterType="java.util.HashMap">
		insert into academy values((select *from (select max(seq)+1 from tbl_board) next), #{name}, #{addr1}, #{addr2}, #{tel}, #{type1}, #{type2},
													#{site}, #{logo}, #{info}, #{pic1}, #{pic2}, #{pic3}, #{pic4}, #{pic5}, 0.0, 0, #{target}, #{subject})
	</insert>
	<select id="academy" resultType="java.util.HashMap">
		select * from academy where num = #{num}
	</select>
	<select id="acanews" parameterType="java.util.HashMap" resultType="java.util.HashMap">
<!-- 		select a.* from (select @rownum := @rownum+1 as rownum, acanews.* from acanews, (select @rownum := 0)r where num = #{num} or num = 0 order by day desc)a limit #{page}, 10 -->
		select aa.* from (select @rownum := @rownum+1 as rownum, a.* from (select * from acanews where num = #{num} order by day asc)a, (select @rownum := 0)r)aa limit #{page}, 10
	</select>
	<select id="newsCount" resultType="java.lang.Integer">
		select count(*) from acanews where num = #{num}
	</select>
	<insert id="review" parameterType="java.util.HashMap">
		insert into review values(#{num}, #{day}, #{subject}, #{subject1}, #{className}, #{teacher}, #{type}, #{point}, #{rate1}, #{rate2},
												#{rate3}, #{rate4}, #{again}, #{writer}, now(), 0, 0)
	</insert>
	<select id="reviewPage" resultType="java.util.HashMap" parameterType="java.util.HashMap">
		select * from review where num = #{num} order by reday desc limit 0, 7
	</select>
	<select id="reviewTop" resultType="java.util.HashMap">
		select * from review where num = #{num} order by recomend desc limit 0, 3
	</select>
	<select id="reviewAll" resultType="java.util.HashMap">
		select * from review where num = #{num}
	</select>
	<select id="again" resultType="java.lang.Integer">
		select count(*) from review where num = #{num} and again = 'y'
	</select>
</mapper>